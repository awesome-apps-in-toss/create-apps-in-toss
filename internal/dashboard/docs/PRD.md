# Barreleye Dashboard PRD v0.3

_One place to manage every mini-app — and a landing page to show the world how it works_

---

## 1. 문제 정의

> "앱인토스 콘솔에 등록할 정보가 어디 있더라? PRD는 만들었나? UT는 돌렸나?"

미니앱 수가 늘어날수록 각 앱의 상태를 파악하는 비용이 커진다. 현재 대시보드는 앱 목록을 나열할 뿐, **무엇이 준비됐고 무엇이 빠졌는지 알 수 없다.**

기존 해결책의 한계:

- 파일 시스템 직접 탐색 → 앱마다 구조가 달라 찾기 어려움
- README에 수동 기록 → 업데이트 안 됨, 흩어져 있음
- 기억에 의존 → 앱이 10개 넘으면 한계

---

## 2. 타깃 유저

### Primary — 로컬 사용자
Barreleye 모노레포를 관리하는 개발자 (1인, 본인)

- 여러 미니앱을 동시에 개발/유지보수
- 앱인토스 콘솔 등록 시 필요한 정보를 모아야 함
- 7단계 출시 스킬(ait-plan → ait-build)을 이미 사용 중
- 브라우저 ↔ 터미널 전환이 잦고 컨텍스트 스위칭 비용이 높음

### Secondary — 데모 방문자 (GitHub Pages)
Barreleye 프로젝트에 관심 있는 외부 개발자

- AI 에이전트 기반 미니앱 개발 워크플로우를 처음 접함
- "이게 뭘 하는 툴인가?"를 5초 안에 파악하고 싶음
- 설치 없이 브라우저에서 대시보드 UI/기능을 탐색하고 싶음

---

## 3. 핵심 가치 제안

**"아이디어에서 앱인토스 출시까지, 7단계 AI 파이프라인으로"**

1. **상태 파악** — 3가지 정보 레이어로 각 앱의 준비 상태를 한눈에
2. **원클릭 실행** — 스킬 버튼 하나로 PRD·에셋·코드·검수를 AI가 처리
3. **콘솔 복붙 준비** — 생성된 결과물을 대시보드에서 바로 확인 + 앱인토스 콘솔 업로드

---

## 4. 두 가지 모드

### 로컬 모드 (`pnpm dev`)
- Express API 서버(포트 3001) + Vite 개발 서버(포트 5173) 동시 실행
- 실제 앱 데이터를 파일 시스템에서 읽음
- 편집·스킬 실행·파일 업로드 등 모든 기능 사용 가능
- SSE로 파일 변경 감지 → 목록 자동 갱신

### 데모 모드 (GitHub Pages)
- API 호출 없이 mock 데이터로 즉시 렌더링
- 상단 배너: "📌 데모 모드 — 실제 기능은 로컬에서 `pnpm dev` 실행 후 사용 가능"
- 편집·스킬 실행 버튼은 비활성화 (tooltip으로 로컬 실행 안내)
- 랜딩페이지로 활용: 방문자가 UI와 기능 구조를 탐색 가능

---

## 5. 정보 레이어 정의

앱마다 관리해야 할 정보는 세 가지 레이어로 구분된다.

### Layer 1 — 빌드 코드 정보 (진실의 원천: 코드)

코드에 정적으로 적혀 있는 빌드/배포 관련 정보. **반드시 존재해야 하며, 코드가 진실의 원천.**

| 항목         | 파일                                     | 비고               |
| ------------ | ---------------------------------------- | ------------------ |
| appName      | `granite.config.ts`                      |                    |
| displayName  | `granite.config.ts › brand.displayName`  |                    |
| primaryColor | `granite.config.ts › brand.primaryColor` |                    |
| 아이콘 URL   | `granite.config.ts › brand.icon`         |                    |
| `.ait` 파일  | `apps/{앱명}/.ait`                       | 앱인토스 등록 여부 |

대시보드 역할: **읽기 전용 표시** (편집 불가 — 코드를 직접 수정해야 함)

가중치: **최대 40점** (appName 10, displayName 10, primaryColor 10, .ait 10)

---

### Layer 2 — 콘솔 등록 에셋 (진실의 원천: `.meta-dashboard.json`)

앱인토스 콘솔에 업로드해야 하는 정보. 저장 위치: `apps/{앱명}/.meta-dashboard.json`

| 항목             | 형식                  | 스킬              |
| ---------------- | --------------------- | ----------------- |
| 앱 로고          | 이미지 600×600        | `ait-assets`      |
| 한국어·영어 앱 이름 | 텍스트             | 수동 입력         |
| 게임/비게임 분류 | 선택                  | 수동 입력         |
| 앱인토스 카테고리 | 예: 생활 > 콘텐츠 > 테스트 | 수동 선택    |
| 부제             | 텍스트                | `ait-plan` 참고   |
| 상세 설명        | 텍스트 (장문)         | `ait-plan` 참고   |
| 검색 키워드      | 쉼표 구분             | `ait-plan` 참고   |
| 가로형 썸네일    | 이미지 1932×828       | `ait-assets`      |
| 세로형 스크린샷  | 이미지 636×1048, 3장+ | `ait-assets`      |

대시보드 역할: **인라인 편집 + 저장**, 이미지 미리보기, 클립보드 복사

가중치: **최대 30점** (로고 10, 텍스트 정보 10, 썸네일 10)

---

### Layer 3 — 개발 헬퍼 (진실의 원천: `docs/`)

없어도 되지만 있으면 더 좋은 앱을 만들 수 있음. 스킬로 자동생성.

| 항목             | 위치                                     | 스킬       |
| ---------------- | ---------------------------------------- | ---------- |
| PRD              | `apps/{앱명}/docs/PRD.md`                | `ait-plan` |
| User Test 리포트 | `apps/{앱명}/docs/user-test/report.md`   | `ait-ut`   |

대시보드 역할: 존재 여부 자동 감지, 마크다운 인라인 뷰어, 경로 편집

가중치: **최대 30점** (PRD 15, UT 15)

---

## 6. 7단계 출시 파이프라인

각 앱 상세 페이지 상단에 파이프라인 섹션이 표시된다.

```
STEP 1    STEP 2    STEP 3       STEP 4    STEP 5    STEP 6    STEP 7
기획  ──▶ 에셋  ──▶ 스캐폴딩 ──▶ TDS  ──▶ 구현  ──▶ 검수  ──▶ 빌드
ait-plan  ait-assets ait-scaffold ait-tds  ait-impl  ait-review ait-build
```

| Step | 스킬             | 모드        | 전제 조건      | 산출물                       |
| ---- | ---------------- | ----------- | -------------- | ---------------------------- |
| 1    | `ait-plan`       | interactive | —              | PRD 문서                     |
| 2    | `ait-assets`     | automated   | —              | 로고, 썸네일, 스크린샷       |
| 3    | `ait-scaffold`   | automated   | PRD (Step 1)   | 프로젝트 구조, granite.config.ts |
| 4    | `ait-tds-setup`  | automated   | 프로젝트 (Step 3) | TDS 패키지, Provider 설정 |
| 5    | `ait-implement`  | automated   | PRD + TDS      | 기능 코드, 라우팅             |
| 6    | `ait-review`     | automated   | 구현 완료      | 검수 리포트                  |
| 7    | `ait-build`      | automated   | 검수 통과      | .ait 번들                    |

- **interactive**: CLI에서 AI와 대화형으로 진행. 대시보드에서 CLI 명령어를 클립보드에 복사 제공
- **automated**: 대시보드 버튼 클릭 → Claude Code 자동 실행 → SSE 로그 스트리밍
- **`/ait-launch`**: 7단계를 순차 실행하는 오케스트레이터. "claude -p /ait-launch" 복사 버튼 제공

파이프라인 진행 상태는 `.meta-dashboard.json`의 `pipelineProgress` 필드에 저장된다.

---

## 7. 완성도 계산 기준

```
Layer 1 (빌드 코드)   — granite.config.ts 존재, displayName, primaryColor, icon: 각 10점 (40점)
Layer 2 (콘솔 에셋)   — 앱 로고, 부제+설명+키워드, 썸네일: 각 10점 (30점)
Layer 3 (개발 헬퍼)   — PRD, UT 리포트: 각 15점 (30점)
총합: 100점
```

---

## 8. 홈 페이지 — 랜딩 + 대시보드

홈은 두 가지 역할을 동시에 수행한다.

### 8-1. 로컬 모드: 앱 관리 대시보드
- 전체 앱 수 / 토스 미니앱 수 통계
- 완성도 필터 (전체 / PRD 없음 / 브랜드 미설정 / 스토어 미등록 / UT 없음)
- 앱 카드 그리드 (이름, 패키지, 완성도 도트, 상태 태그)

### 8-2. 데모 모드(GitHub Pages): 랜딩 페이지

방문자가 "이게 뭔지"를 5초 안에 이해할 수 있어야 한다.

**랜딩 페이지가 전달해야 하는 메시지 (우선순위 순):**

1. **한 줄 설명**: "AI로 토스 미니앱을 기획부터 출시까지 7단계로"
2. **핵심 차별점**: 대시보드에서 앱 상태를 보고, 버튼 하나로 AI 스킬 실행
3. **실제 모습**: 목 데이터로 채워진 앱 카드들이 곧 데모

**UI 구성 (데모 모드 전용 상단 섹션):**
```
┌─────────────────────────────────────────────────────────────┐
│  🛢 Barreleye                                               │
│                                                             │
│  AI로 토스 미니앱을 기획부터 출시까지                        │
│  7단계 파이프라인으로 관리하세요                             │
│                                                             │
│  [GitHub에서 보기]  [로컬에서 시작하기 →]                   │
│                                                             │
│  ────────────────────────────────────────────────────────  │
│  전체 앱 3    토스 미니앱 3    평균 완성도 49%               │
└─────────────────────────────────────────────────────────────┘
```

**로컬에서 시작하기 버튼**: GitHub 저장소 README의 Quick Start 섹션 링크

---

## 9. 주요 기능 (현재 구현 상태)

### ✅ 완료

| 기능                  | 설명                                                        |
| --------------------- | ----------------------------------------------------------- |
| 앱 목록 홈            | 카드 그리드, 완성도 도트, 상태 태그, 필터                   |
| 앱 상세 페이지        | 3개 레이어 섹션 + 파이프라인 섹션                           |
| 좌측 사이드바         | 앱 목록 퀵점프, 홈 네비게이션                               |
| Layer 1 읽기          | granite.config.ts 파싱 (읽기 전용)                          |
| Layer 2 편집          | `.meta-dashboard.json` 인라인 편집 + 저장                   |
| Layer 2 이미지 미리보기 | 로고, 썸네일, 스크린샷 인라인 표시                        |
| Layer 3 감지          | PRD/UT 파일 존재 여부 자동 감지 + 마크다운 뷰어             |
| 7단계 파이프라인 UI   | 미니 스테퍼 + 개별 단계 펼침 + 진행 상태 저장              |
| 스킬 트리거           | 버튼 클릭 → Claude Code CLI 자동 실행                       |
| SSE 로그 스트리밍     | 스킬 실행 중 실시간 로그 출력                               |
| 파일 변경 감지        | chokidar → 목록 자동 갱신                                   |
| PRD 업로드            | drag & drop 또는 파일 선택으로 .md 업로드                   |
| 클립보드 복사         | Layer 2 텍스트 필드 복사 + CLI 명령어 복사                  |
| GitHub Pages 배포     | main 머지 시 자동 배포 (GitHub Actions)                     |
| 데모 모드             | 정적 배포 시 mock 데이터 + 쓰기 기능 비활성화               |

### 🔲 다음

| 기능                     | 설명                                          |
| ------------------------ | --------------------------------------------- |
| 홈 랜딩 섹션             | 데모 모드 전용 Hero 섹션 (이 PRD 8절 참고)    |
| 스킬 실행 히스토리       | 언제 어떤 스킬을 실행했는지 기록              |
| 앱인토스 카테고리 드롭다운 | 콘솔 구조 파싱으로 선택형 UI 제공           |
| 스크린샷 자동 생성       | Playwright로 앱 캡처                          |

### ❌ Won't

- 실제 앱 실행 (dev server 임베딩)
- 다중 사용자/권한 관리

---

## 10. UX 플로우

### 레이아웃

```
┌──────────────┬────────────────────────────────────────┐
│  🛢 Barreleye│  전체 앱 3    토스 미니앱 3             │
│  ──────────  │  ─────────────────────────────────      │
│  📊 홈       │                                         │
│              │  ┌──────────┐ ┌──────────┐ ┌────────┐  │
│  앱 목록     │  │ 소개팅   │ │ 샘플 앱  │ │ 가계부 │  │
│  ├ 소개팅    │  │ 발주서   │ │          │ │        │  │
│  ├ 샘플 앱   │  │ ●●●●○ 82%│ │ ●●●○○ 57%│ │ ●○○○○  │  │
│  ├ 가계부    │  └──────────┘ └──────────┘ └────────┘  │
│              │                                         │
└──────────────┴────────────────────────────────────────┘
```

### 앱 상세 페이지 — 파이프라인 섹션

```
출시 파이프라인                    [claude -p /ait-launch]  [접기]

  ✓ ─── ✓ ─── ✓ ─── ✓ ─── ✓ ─── ○ ─── ○
 기획   에셋  스캐폴 TDS   구현   검수   빌드

  STEP 1  기획   정책 검토 + PRD 생성 → PRD 문서        [기획하기]
  STEP 2  에셋   이미지/텍스트 리소스 생성 → 로고, 썸네일  [실행]
  ...
```

### 스킬 트리거 플로우 (로컬 모드)

```
[실행] 클릭
→ GET /api/run-skill/stream?skill=ait-assets&app=dating-quiz
→ Express가 Claude Code CLI 실행 (cwd: apps/dating-quiz)
→ SSE로 로그 스트리밍
→ 완료 시 chokidar가 파일 변경 감지 → 목록 자동 갱신
```

### 스킬 트리거 플로우 (데모 모드)

```
[실행] 버튼 (disabled)
→ hover: "로컬에서 pnpm dev 실행 시 사용 가능"
```

---

## 11. 기술 스택

### 프론트엔드

```
React 18 + TypeScript + Vite 6
React Router v7
```

### 백엔드 (로컬 서버)

```
Express 5          API 서버 (포트 3001)
child_process      Claude Code CLI 실행
SSE                실시간 로그 스트리밍
chokidar           파일 변경 감지
concurrently       pnpm dev 시 프론트 + 서버 동시 실행
```

### 인프라

```
GitHub Actions     main 머지 시 GitHub Pages 자동 배포
Turborepo          모노레포 빌드 오케스트레이션
```

### 데이터 스키마

**Layer 2** — `apps/{앱명}/.meta-dashboard.json`

```json
{
  "version": 1,
  "nameKo": "소개팅 발주서",
  "nameEn": "Dating Order Form",
  "isGame": false,
  "aitCategory": "생활 > 콘텐츠 > 테스트",
  "subtitle": "내 이상형 조건을 발주서처럼 작성해보세요",
  "description": "...",
  "keywords": ["소개팅", "이상형"],
  "logoPath": ".meta/assets/logo.png",
  "thumbnailPath": ".meta/assets/thumbnail-wide.png",
  "screenshotPaths": [".meta/assets/screenshot-1.png"],
  "prdPath": "docs/PRD.md",
  "utPath": "docs/user-test/report.md",
  "pipelineProgress": {
    "1": { "completedAt": "2026-04-01T09:00:00Z", "artifacts": { "prd": "docs/PRD.md" } },
    "2": { "completedAt": "2026-04-01T10:30:00Z" }
  },
  "updatedAt": "2026-04-08T11:00:00Z"
}
```

**Layer 3 감지 규칙**

```
PRD        apps/{앱명}/docs/PRD.md  (또는 prdPath 필드)
UT 리포트  apps/{앱명}/docs/user-test/report.md  (또는 utPath 필드)
```

---

## 12. 리스크 및 가정

| 리스크                      | 가능성 | 대응                                            |
| --------------------------- | ------ | ----------------------------------------------- |
| granite.config.ts 파싱 실패 | 낮음   | 정규식 실패 시 null 처리, 섹션 미표시           |
| Claude CLI 경로 문제        | 중간   | `which claude`로 경로 검증 후 실행              |
| 스킬 실행 중 에러           | 높음   | stderr 스트리밍 + 실패 상태 표시                |
| 이미지 에셋 경로 싱크       | 중간   | 앱 폴더명 기준 상대경로 통일                    |

**가정**

- Claude Code CLI가 로컬에 설치되어 있고 PATH에 등록됨
- 스킬은 앱 폴더 컨텍스트에서 실행 (`cwd: apps/{앱명}`)
- `.meta-dashboard.json`은 Git에 커밋 (콘솔 정보 버전 관리)
